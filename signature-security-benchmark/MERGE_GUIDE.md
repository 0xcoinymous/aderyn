# Merge Guide

All Solidity target files live directly in `contracts/`. Preserve globally reserved prefixes: SR_, SM_, NM_, E712_, E1271_, SF_. Append manifest rows without changing the 21-column schema. Never overwrite existing case IDs, filenames, target contract names, or helper declarations. Support files are compiled but excluded from target-case scoring.
